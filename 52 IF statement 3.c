#include<stdio.h>
int main()
{
    int x=4;

    if(x>=5)
    {
        printf("x is greater than or equal to five");
        
    }

    printf("x is equal to four");

    return 0;
}

// if the IF condition is false only the first statement after IF will be skipped and rest other following the first statement will be executed.

// if the IF condition is true then both the statement get executed.
